## Overview
A simple **Java implementation** of the SmartOven system designed in Lab 1.  
Now the project is split into **separate class files** for clarity, following OOP principles.

---

## Files
- `SmartOven.java` — main class (core functionality)
- `OvenDoor.java` — door control
- `UserAccount.java` — user interactions
- `Main.java` — entry point (demo)
- `README.md` — documentation

---

## Reflection
Compared to Lab 1:
- Classes are now implemented in real Java code.
- Project split into multiple files for better readability.
- Added `checkStatus()` to display the oven’s current state.
- Simplified logic to basic print statements to simulate behavior.

---

## AI Feedback
AI suggested:
- Add getters/setters for encapsulation.  
- Include `checkStatus()` to print oven info (I implemented this).  
- Keep structure simple for readability — fully adopted.

---

## How to Run

1. Open terminal inside project folder  
2. Compile all files:
   ```bash
   javac *.java
